# --- Vexa Environment ---
# Copy to repo root as .env:  cp deploy/env-example .env
# Then edit the REQUIRED fields below.

# ============================================================================
# REQUIRED — you MUST set these
# ============================================================================

# Transcription service
# Get a key at vexa.ai OR self-host services/transcription-service/ with GPU
TRANSCRIPTION_SERVICE_URL=https://transcription.vexa.ai/v1/audio/transcriptions
TRANSCRIPTION_SERVICE_TOKEN=your-token
# OR any external OpenAI-compatible STT provider. Set PROVIDER=openai to use
# the strict OpenAI dialect (standard form fields only, timestamp_granularities[]).
#   Groq:   TRANSCRIPTION_SERVICE_URL=https://api.groq.com/openai/v1/audio/transcriptions
#           TRANSCRIPTION_SERVICE_MODEL=whisper-large-v3-turbo
#   OpenAI: TRANSCRIPTION_SERVICE_URL=https://api.openai.com/v1/audio/transcriptions
#           TRANSCRIPTION_SERVICE_MODEL=whisper-1
# TRANSCRIPTION_SERVICE_PROVIDER=openai
# TRANSCRIPTION_SERVICE_MODEL=

# Default bot display name in meetings (per-request bot_name overrides).
# Unset = "VexaBot-<random>".
# BOT_DEFAULT_NAME=

# Admission policy (ms). Unset = system defaults (15 min admission wait).
# Set BOT_MAX_WAIT_FOR_ADMISSION_MS short for a "default to deny" posture — the
# bot gives up quickly if ignored; explicit host denial always leaves instantly.
# BOT_MAX_WAIT_FOR_ADMISSION_MS=60000
# BOT_MAX_BOT_TIME_MS=7200000
# BOT_MAX_TIME_LEFT_ALONE_MS=900000
# BOT_NO_ONE_JOINED_TIMEOUT_MS=120000

# ============================================================================
# Ports — change these to avoid conflicts with other stacks
# ============================================================================
API_GATEWAY_HOST_PORT=8056
ADMIN_API_PORT=8057
RUNTIME_API_PORT=8090
AGENT_API_PORT=8100
CALENDAR_SERVICE_PORT=8050
DASHBOARD_HOST_PORT=3001
POSTGRES_HOST_PORT=5458
MCP_HOST_PORT=18888
MINIO_HOST_PORT=9000
MINIO_CONSOLE_HOST_PORT=9001

# ============================================================================
# Defaults — work as-is for local dev
# ============================================================================

# Admin
ADMIN_TOKEN=changeme

# Image tag — pre-built from DockerHub by default.
# `make build` writes a local tag to .last-tag which overrides this.
IMAGE_TAG=latest
BROWSER_IMAGE=vexaai/vexa-bot:latest


# Database (embedded postgres by default)
DB_NAME=vexa
DB_USER=postgres
DB_PASSWORD=postgres
# For external DB, uncomment:
# REMOTE_DB=true
# DB_HOST=your-postgres-host
# DB_PORT=5432

# Storage (embedded minio by default)
STORAGE_BACKEND=minio
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=vexa-access-key
MINIO_SECRET_KEY=vexa-secret-key
MINIO_BUCKET=vexa
MINIO_SECURE=false

# Local GPU transcription (uncomment to self-host)
# LOCAL_TRANSCRIPTION=true

# TTS uses Piper (local, no API key needed)
# Voice models auto-download from HuggingFace on first use

# Agent API — Claude credentials (optional, for agent-api service)
CLAUDE_CREDENTIALS_PATH=
CLAUDE_JSON_PATH=

# Billing hooks (optional — for hosted service)
# POST_MEETING_HOOKS=https://your-billing-webhook
